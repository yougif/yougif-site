name = "yougif"
compatibility_date = "2025-12-01"
pages_build_output_dir = "dist"

[vars]
PUBLIC_SITE_MODE = "cloudflare"

# R2 is intentionally not bound yet. Add it later if public media storage
# needs to move out of the Git repo.

[[d1_databases]]
binding = "YOUGIF_DB"
database_name = "yougif-site"
database_id = "bb8e04a1-63bf-4e25-82d3-63a3ff1a883f"
